import React from "react";
import ReactDOM from "react-dom/client";
import SparrowSchool from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SparrowSchool />
  </React.StrictMode>
);
